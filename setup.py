from setuptools import setup, find_packages

setup(
    name='dict_wash',
    description='wash your dicts',
    long_description='takes in a dict and returns a clean dict',
    version='0.69.0',
    install_requires=[],
    packages=find_packages(),
)
